<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Electrical QC Check"
        Height="600"
        Width="1050"
        MinHeight="480"
        MinWidth="860"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource CED.Brush.WindowBackground}"
        Activated="window_activated">
  <Window.Resources>
    <Style x:Key="QC.TextCell" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource CED.Brush.PrimaryText}"/>
      <Setter Property="Margin" Value="6,2,6,2"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
      <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>
    <Style x:Key="QC.TextCell.Center" TargetType="TextBlock" BasedOn="{StaticResource QC.TextCell}">
      <Setter Property="TextAlignment" Value="Center"/>
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>
    <Style x:Key="QC.TextCell.Wrap" TargetType="TextBlock" BasedOn="{StaticResource QC.TextCell}">
      <Setter Property="TextWrapping" Value="Wrap"/>
      <Setter Property="TextTrimming" Value="None"/>
    </Style>

    <ControlTemplate x:Key="QC.DetailCell.Template" TargetType="DataGridCell">
      <Grid Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
        <Border HorizontalAlignment="Right" Width="1" Background="{DynamicResource CED.Brush.DataGridCellBorder}"/>
        <Border VerticalAlignment="Bottom" Height="1" Background="{DynamicResource CED.Brush.DataGridCellBorder}"/>
        <Border x:Name="GroupTopDivider" VerticalAlignment="Top" Height="1" Background="{DynamicResource CED.Brush.DataGridDividerStrong}" Visibility="Collapsed"/>
        <Border x:Name="GroupBottomDivider" VerticalAlignment="Bottom" Height="1" Background="{DynamicResource CED.Brush.DataGridDividerStrong}" Visibility="Collapsed"/>
      </Grid>
      <ControlTemplate.Triggers>
        <DataTrigger Binding="{Binding is_group_first}" Value="True">
          <Setter TargetName="GroupTopDivider" Property="Visibility" Value="Visible"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding is_group_last}" Value="True">
          <Setter TargetName="GroupBottomDivider" Property="Visibility" Value="Visible"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding is_group_single}" Value="True">
          <Setter TargetName="GroupTopDivider" Property="Visibility" Value="Visible"/>
          <Setter TargetName="GroupBottomDivider" Property="Visibility" Value="Visible"/>
        </DataTrigger>
      </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="QC.MergedCell.Template" TargetType="DataGridCell">
      <Grid Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
        <Border HorizontalAlignment="Right" Width="1" Background="{DynamicResource CED.Brush.DataGridCellBorder}"/>
        <Border x:Name="MergedBottomDivider" VerticalAlignment="Bottom" Height="1" Background="{DynamicResource CED.Brush.DataGridCellBorder}" Visibility="Collapsed"/>
        <Border x:Name="GroupTopDivider" VerticalAlignment="Top" Height="1" Background="{DynamicResource CED.Brush.DataGridDividerStrong}" Visibility="Collapsed"/>
        <Border x:Name="GroupBottomDivider" VerticalAlignment="Bottom" Height="1" Background="{DynamicResource CED.Brush.DataGridDividerStrong}" Visibility="Collapsed"/>
      </Grid>
      <ControlTemplate.Triggers>
        <DataTrigger Binding="{Binding is_group_last}" Value="True">
          <Setter TargetName="MergedBottomDivider" Property="Visibility" Value="Visible"/>
          <Setter TargetName="GroupBottomDivider" Property="Visibility" Value="Visible"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding is_group_first}" Value="True">
          <Setter TargetName="GroupTopDivider" Property="Visibility" Value="Visible"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding is_group_single}" Value="True">
          <Setter TargetName="MergedBottomDivider" Property="Visibility" Value="Visible"/>
          <Setter TargetName="GroupTopDivider" Property="Visibility" Value="Visible"/>
          <Setter TargetName="GroupBottomDivider" Property="Visibility" Value="Visible"/>
        </DataTrigger>
      </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="QC.GridCell" TargetType="DataGridCell">
      <Setter Property="Background" Value="{DynamicResource CED.Brush.ListItemBackground}"/>
      <Setter Property="Foreground" Value="{DynamicResource CED.Brush.DataGridRowForeground}"/>
      <Setter Property="Template" Value="{StaticResource QC.DetailCell.Template}"/>
      <Setter Property="Padding" Value="0"/>
      <Setter Property="Focusable" Value="False"/>
      <Setter Property="IsTabStop" Value="False"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="SnapsToDevicePixels" Value="True"/>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="{DynamicResource CED.Brush.DataGridSelectionBackground}"/>
          <Setter Property="Foreground" Value="{DynamicResource CED.Brush.DataGridSelectionForeground}"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style x:Key="QC.MergedCell" TargetType="DataGridCell" BasedOn="{StaticResource QC.GridCell}">
      <Setter Property="Background" Value="{DynamicResource CED.Brush.DataGridReadOnlyBackground}"/>
      <Setter Property="Template" Value="{StaticResource QC.MergedCell.Template}"/>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="{DynamicResource CED.Brush.DataGridSelectionBackground}"/>
          <Setter Property="Foreground" Value="{DynamicResource CED.Brush.DataGridSelectionForeground}"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style x:Key="QC.DetailCell" TargetType="DataGridCell" BasedOn="{StaticResource QC.GridCell}">
      <Setter Property="Background" Value="{DynamicResource CED.Brush.DataGridAltRow}"/>
    </Style>
    <Style x:Key="QC.SeverityCell" TargetType="DataGridCell" BasedOn="{StaticResource QC.MergedCell}">
      <Style.Triggers>
        <DataTrigger Binding="{Binding severity}" Value="INFO">
          <Setter Property="Background" Value="{DynamicResource CED.Brush.BadgeInfoBackground}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding severity}" Value="LOW">
          <Setter Property="Background" Value="{DynamicResource CED.Brush.DataGridChangedBackground}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding severity}" Value="MEDIUM">
          <Setter Property="Background" Value="{DynamicResource CED.Brush.DataGridWarningBackground}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding severity}" Value="HIGH">
          <Setter Property="Background" Value="{DynamicResource CED.Brush.DataGridErrorBackground}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding severity}" Value="CRITICAL">
          <Setter Property="Background" Value="{DynamicResource CED.Brush.DataGridErrorBackground}"/>
        </DataTrigger>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="{DynamicResource CED.Brush.DataGridSelectionBackground}"/>
          <Setter Property="Foreground" Value="{DynamicResource CED.Brush.DataGridSelectionForeground}"/>
        </Trigger>
      </Style.Triggers>
    </Style>
  </Window.Resources>
  <Grid Margin="{DynamicResource CED.Thickness.ContentMargin}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <StackPanel Grid.Column="0">
        <TextBlock Text="Electrical QC Check" Style="{DynamicResource CED.Text.Title}"/>
        <TextBlock Name="DocumentText" Text="Document: -" Style="{DynamicResource CED.Text.Secondary}" Margin="0,2,0,0"/>
      </StackPanel>
      <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
        <Button Name="RefreshButton" Content="Refresh" Style="{DynamicResource CED.Button.Base}" Width="82" Margin="0,0,6,0" ToolTip="Re-run the QC scan for the active document and refresh this grid." Click="refresh_clicked"/>
        <Button Name="ColumnsButton" Content="Columns" Style="{DynamicResource CED.Button.Base}" Width="88" Margin="0,0,6,0" ToolTip="Show or hide grid columns." Click="columns_clicked"/>
        <Button Name="ExportButton" Content="Export CSV" Style="{DynamicResource CED.Button.Primary}" Width="96" ToolTip="Export all QC columns to a CSV file." Click="export_clicked"/>
      </StackPanel>
    </Grid>

    <Grid Grid.Row="1">
      <Border BorderBrush="{DynamicResource CED.Brush.DataGridBorder}"
              BorderThickness="1"
              Background="{DynamicResource CED.Brush.ListBackground}">
        <DataGrid Name="QCGrid"
                  Style="{DynamicResource CED.DataGrid.Display}"
                  ColumnHeaderStyle="{DynamicResource CED.DataGrid.Header.WrapTall}"
                  RowStyle="{DynamicResource CED.DataGrid.Row.Display.NeutralSelection}"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Visibility="Hidden"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  CanUserReorderColumns="True"
                  CanUserSortColumns="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  VirtualizingPanel.ScrollUnit="Pixel"
                  VirtualizingPanel.CacheLength="2"
                  VirtualizingPanel.CacheLengthUnit="Page"
                  ScrollViewer.CanContentScroll="True"
                  Loaded="grid_loaded"
                  SelectionChanged="grid_selection_changed"
                  Sorting="grid_sorting">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Category" Width="11*" MinWidth="78" Binding="{Binding display_category}" SortMemberPath="category" ElementStyle="{StaticResource QC.TextCell}" CellStyle="{StaticResource QC.MergedCell}"/>
            <DataGridTextColumn Header="Alert ID" Width="16*" MinWidth="105" Binding="{Binding display_check_id}" SortMemberPath="check_id" ElementStyle="{StaticResource QC.TextCell}" CellStyle="{StaticResource QC.MergedCell}"/>
            <DataGridTextColumn Header="Severity" Width="7*" MinWidth="50" Binding="{Binding display_severity}" SortMemberPath="severity" ElementStyle="{StaticResource QC.TextCell.Center}" CellStyle="{StaticResource QC.SeverityCell}"/>
            <DataGridTemplateColumn Header="Issue" Width="12*" MinWidth="115" SortMemberPath="issue" CellStyle="{StaticResource QC.MergedCell}">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding display_issue}" Style="{StaticResource QC.TextCell.Wrap}"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTemplateColumn Header="Recommended Action" Width="17*" MinWidth="120" SortMemberPath="recommended_action" CellStyle="{StaticResource QC.MergedCell}">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding display_recommended_action}" Style="{StaticResource QC.TextCell.Wrap}"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTemplateColumn Header="Observed" Width="13*" MinWidth="95" SortMemberPath="observed" CellStyle="{StaticResource QC.DetailCell}">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding observed}" Style="{StaticResource QC.TextCell.Wrap}"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTemplateColumn Header="Primary Element" Width="13*" MinWidth="110" SortMemberPath="primary_element" CellStyle="{StaticResource QC.DetailCell}">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding primary_element}" Style="{StaticResource QC.TextCell.Wrap}"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTemplateColumn Header="Secondary Element" Width="13*" MinWidth="110" SortMemberPath="secondary_element" CellStyle="{StaticResource QC.DetailCell}">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding secondary_element}" Style="{StaticResource QC.TextCell.Wrap}"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
          </DataGrid.Columns>
        </DataGrid>
      </Border>
      <Grid Name="LoadingOverlay"
            Background="{DynamicResource CED.Brush.ListBackground}"
            Visibility="Collapsed">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="260">
          <TextBlock Text="Loading QC results" Style="{DynamicResource CED.Text.Secondary}" HorizontalAlignment="Center" Margin="0,0,0,8"/>
          <ProgressBar Name="LoadingProgress" Minimum="0" Maximum="100" Value="0" Height="14"/>
        </StackPanel>
      </Grid>
    </Grid>

    <Grid Grid.Row="2" Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
        <TextBlock Name="CountText" Text="0 issues" Style="{DynamicResource CED.Text.Secondary}" VerticalAlignment="Center"/>
        <TextBlock Text="  |  " Style="{DynamicResource CED.Text.Secondary}" VerticalAlignment="Center"/>
        <TextBlock Name="StatusText" Text="Ready" Style="{DynamicResource CED.Text.Secondary}" VerticalAlignment="Center"/>
      </StackPanel>
      <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Name="SelectPrimaryButton" Content="Select Primary" Style="{DynamicResource CED.Button.Base}" Width="116" Margin="0,0,6,0" Click="select_primary_clicked"/>
        <Button Name="SelectSecondaryButton" Content="Select Secondary" Style="{DynamicResource CED.Button.Base}" Width="128" Margin="0,0,12,0" Click="select_secondary_clicked"/>
        <Button Content="Close" Style="{DynamicResource CED.Button.Base}" Width="76" Click="close_clicked"/>
      </StackPanel>
    </Grid>
  </Grid>
</Window>
